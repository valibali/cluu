[package]
name = "xtask"
version = "0.1.0"
edition = "2021"

# xtask uses std - it runs on the host
[dependencies]
anyhow = "1.0"
clap = { version = "4.4", features = ["derive"] }
sha2 = "0.10"
terminal_size = "0.3"
