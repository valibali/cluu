# Cargo configuration for CLUU microkernel

# Note: build-std is only for our custom targets, not for host testing

# Kernel target configuration
[target.x86_64-cluu-kernel]
rustflags = [
    "-C", "opt-level=0",
    "-C", "lto=off",
    "-C", "force-frame-pointers=yes",
    "-C", "link-arg=-nostdlib",
    "-C", "link-arg=-static",
]

# Userspace target configuration
# Note: opt-level is not set here to allow --release to use opt-level=3
# For debug builds, cargo will use opt-level=0 by default
[target.x86_64-cluu-user]
rustflags = [
    "-C", "lto=off",
    "-C", "link-arg=-nostdlib",
    "-C", "link-arg=-static",
]

# Cargo alias for xtask
[alias]
xtask = "run --package xtask --"
