[package]
name = "cluu-tail"
version = "0.1.0"
edition = "2021"
description = "CLUU tail utility"
authors = ["CLUU Team", "Balazs Valkony"]
license = "MIT"

[dependencies]
libcluu = { path = "../libcluu", features = ["posix"] }

[[bin]]
name = "tail"
path = "src/main.rs"
