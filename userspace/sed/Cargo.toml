[package]
name = "cluu-sed"
version = "0.1.0"
edition = "2021"
description = "CLUU sed stream editor"
authors = ["CLUU Team", "Balazs Valkony"]
license = "MIT"

[dependencies]
libcluu = { path = "../libcluu", features = ["posix"] }

[[bin]]
name = "sed"
path = "src/main.rs"
