[package]
name = "cluu-regdeny"
version = "0.1.0"
edition = "2021"
description = "CLUU regdeny probe"
authors = ["CLUU Team"]
license = "MIT"

[dependencies]
libcluu = { path = "../../libcluu", features = ["posix"] }

[[bin]]
name = "regdeny"
path = "src/main.rs"
