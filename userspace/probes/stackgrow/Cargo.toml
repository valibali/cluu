[package]
name = "cluu-stackgrow"
version = "0.1.0"
edition = "2021"
description = "CLUU stack growth (M10) demand-paging probe"
authors = ["CLUU Team"]
license = "MIT"

[dependencies]
libcluu = { path = "../../libcluu", features = ["posix"] }

[[bin]]
name = "stackgrow"
path = "src/main.rs"
