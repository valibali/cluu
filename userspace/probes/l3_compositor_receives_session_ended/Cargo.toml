[package]
name = "cluu-l3-compositor-receives-session-ended"
version = "0.1.0"
edition = "2021"
description = "Spec 3 acceptance marker: compositor receives SESSION_ENDED"
authors = ["CLUU Team", "Balazs Valkony"]
license = "MIT"

[dependencies]
libcluu = { path = "../../libcluu", features = ["posix"] }
cluu_proto = { path = "../../cluu_proto" }
postcard = { workspace = true }

[[bin]]
name = "l3_compositor_receives_session_ended"
path = "src/main.rs"