[package]
name = "cluu-timetick-probe"
version = "0.1.0"
edition = "2021"
description = "Periodic-tick subscription smoke test (TIME_SUBSCRIBE_PERIODIC / TIME_TICK)"
authors = ["CLUU Team", "Balazs Valkony"]
license = "MIT"

[dependencies]
libcluu = { path = "../../libcluu" }

[[bin]]
name = "timetick_probe"
path = "src/main.rs"
