[package]
name = "cluu-l2-pts-listing"
version = "0.1.0"
edition = "2021"
description = "Plan 2 Task 8 marker: /dev/pts is enumerable from a shell child"
authors = ["CLUU Team", "Balazs Valkony"]
license = "MIT"

[dependencies]
libcluu = { path = "../../libcluu", features = ["posix"] }

[[bin]]
name = "l2_pts_listing"
path = "src/main.rs"
