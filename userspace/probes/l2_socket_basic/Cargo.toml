[package]
name = "cluu-l2-socket-basic"
version = "0.1.0"
edition = "2021"
description = "BSD socket API smoke test — loopback TCP echo"
authors = ["CLUU Team", "Balazs Valkony"]
license = "MIT"

[dependencies]
libcluu = { path = "../../libcluu", features = ["posix"] }

[[bin]]
name = "l2_socket_basic"
path = "src/main.rs"
