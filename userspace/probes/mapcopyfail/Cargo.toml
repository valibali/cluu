[package]
name = "cluu-mapcopyfail"
version = "0.1.0"
edition = "2021"
description = "CLUU mapcopyfail probe"
authors = ["CLUU Team"]
license = "MIT"

[dependencies]
libcluu = { path = "../../libcluu", features = ["posix"] }

[[bin]]
name = "mapcopyfail"
path = "src/main.rs"
