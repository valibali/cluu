[package]
name = "cluu-l2-sigtstp-delivered"
version = "0.1.0"
edition = "2021"
description = "Spec 2 marker: verify SIGTSTP delivery"
authors = ["CLUU Team", "Balazs Valkony"]
license = "MIT"

[dependencies]
libcluu = { path = "../../libcluu", features = ["posix"] }

[[bin]]
name = "l2_sigtstp_delivered"
path = "src/main.rs"