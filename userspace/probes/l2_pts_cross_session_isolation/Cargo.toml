[package]
name = "cluu-l2-pts-cross-session-isolation"
version = "0.1.0"
edition = "2021"
description = "Spec 2 marker: pts cross-session isolation"
authors = ["CLUU Team", "Balazs Valkony"]
license = "MIT"

[dependencies]
libcluu = { path = "../../libcluu", features = ["posix"] }

[[bin]]
name = "l2_pts_cross_session_isolation"
path = "src/main.rs"