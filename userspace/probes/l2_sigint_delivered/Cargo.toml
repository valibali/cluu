[package]
name = "cluu-l2-sigint-delivered"
version = "0.1.0"
edition = "2021"
description = "Spec 2 marker: verify SIGINT delivery"
authors = ["CLUU Team", "Balazs Valkony"]
license = "MIT"

[dependencies]
libcluu = { path = "../../libcluu", features = ["posix"] }

[[bin]]
name = "l2_sigint_delivered"
path = "src/main.rs"