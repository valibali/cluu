[package]
name = "cluu-l3-session-derive-narrow"
version = "0.1.0"
edition = "2021"
description = "Spec 3 acceptance marker: session derive token with subset rights"
authors = ["CLUU Team", "Balazs Valkony"]
license = "MIT"

[dependencies]
libcluu = { path = "../../libcluu", features = ["posix"] }
cluu_wire = { path = "../../cluu_wire" }
postcard = { workspace = true }

[[bin]]
name = "l3_session_derive_narrow"
path = "src/main.rs"