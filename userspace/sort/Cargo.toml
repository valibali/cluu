[package]
name = "cluu-sort"
version = "0.1.0"
edition = "2021"
description = "CLUU sort utility"
authors = ["CLUU Team", "Balazs Valkony"]
license = "MIT"

[dependencies]
libcluu = { path = "../libcluu", features = ["posix"] }

[[bin]]
name = "sort"
path = "src/main.rs"
