[package]
name = "cluu-mv"
version = "0.1.0"
edition = "2021"
description = "CLUU mv utility"
authors = ["CLUU Team", "Balazs Valkony"]
license = "MIT"

[dependencies]
libcluu = { path = "../libcluu", features = ["posix"] }

[[bin]]
name = "mv"
path = "src/main.rs"
