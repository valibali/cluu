[package]
name = "cluu-compositor"
version = "0.1.0"
edition = "2021"
description = "CLUU TUI compositor — owns one VT, draws floating windows"
authors = ["CLUU Team", "Balazs Valkony"]
license = "MIT"

[dependencies]
libcluu = { path = "../libcluu" }

[[bin]]
name = "compositor"
path = "src/main.rs"
