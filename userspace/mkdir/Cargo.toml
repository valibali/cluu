[package]
name = "cluu-mkdir"
version = "0.1.0"
edition = "2021"
description = "CLUU mkdir utility"
authors = ["CLUU Team", "Balazs Valkony"]
license = "MIT"

[dependencies]
libcluu = { path = "../libcluu", features = ["posix"] }

[[bin]]
name = "mkdir"
path = "src/main.rs"
