[package]
name = "cluu-mountprobe"
version = "0.1.0"
edition = "2021"
description = "CLUU MOUNT /tmp private isolation smoke test"
authors = ["CLUU Team", "Balazs Valkony"]
license = "MIT"

[dependencies]
libcluu = { path = "../libcluu", features = ["posix"] }

[[bin]]
name = "mountprobe"
path = "src/main.rs"
