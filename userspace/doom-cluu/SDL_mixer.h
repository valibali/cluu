/* Stub SDL_mixer.h — DOOM's i_sound.c includes this under FEATURE_SOUND.
 * The CLUU port uses its own in-process SFX mixer (i_cluu_sound.c) and
 * does not link against SDL_mixer.  This stub satisfies the include. */
#ifndef SDL_MIXER_H
#define SDL_MIXER_H
#endif
