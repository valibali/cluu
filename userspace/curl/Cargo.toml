[package]
name = "cluu-curl"
version = "0.1.0"
edition = "2021"
description = "CLUU HTTP client (curl-like)"
authors = ["CLUU Team", "Balazs Valkony"]
license = "MIT"

[dependencies]
libcluu = { path = "../libcluu", features = ["posix"] }

[[bin]]
name = "curl"
path = "src/main.rs"
