[package]
name = "cluu-ping"
version = "0.1.0"
edition = "2021"
description = "CLUU ICMP ping utility (echo via netd)"
authors = ["CLUU Team", "Balazs Valkony"]
license = "MIT"

[dependencies]
libcluu = { path = "../libcluu" }

[[bin]]
name = "ping"
path = "src/main.rs"
