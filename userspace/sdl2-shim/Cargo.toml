[package]
name = "sdl2-cluu"
version = "0.1.0"
edition = "2021"

[lib]
name = "sdl2_cluu"
crate-type = ["staticlib"]

[dependencies]
libcluu = { path = "../libcluu", features = ["c-runtime"] }
cluu_wire = { path = "../cluu_wire" }

[features]
bench = []
