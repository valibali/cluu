[package]
name = "cluu-inputd"
version = "0.1.0"
edition = "2021"
description = "CLUU input daemon — input routing + /dev/input/* server"
authors = ["CLUU Team", "Balazs Valkony"]
license = "MIT"

[dependencies]
libcluu = { path = "../libcluu" }

[[bin]]
name = "inputd"
path = "src/main.rs"
