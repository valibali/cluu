[package]
name = "cluu-basename"
version = "0.1.0"
edition = "2021"
description = "CLUU basename utility"
authors = ["CLUU Team", "Balazs Valkony"]
license = "MIT"

[dependencies]
libcluu = { path = "../libcluu", features = ["posix"] }

[[bin]]
name = "basename"
path = "src/main.rs"
