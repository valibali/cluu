[[service]]
image = "console"

[[service]]
image = "vtmgr"

[[service]]
image = "kbd"

[[service]]
image = "compositor"
session_mode = 0

# login is spawned by compositor (spawn_login_window) — not via autostart,
# to avoid double-spawning two login windows.
